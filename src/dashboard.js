import React, {Component} from 'react';

function Dashboard {
    return (
        // Header
        // List of articles with delete button + delete bytton on click will ball backend.
        // Addition form with add button
    )

}